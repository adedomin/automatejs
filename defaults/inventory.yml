# an array of server objects
# objects contain a name, a user
# and a keyfile which can be used to remote into
# sudo should be passwordless

localhost:
  - name: localhost
    user: automatejs
    keyfile: '/Users/prussian/.ssh/id_rsa'
